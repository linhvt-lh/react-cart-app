import Styled from "styled-components";

export const HeaderStyle = Styled.div`
    .mini-cart{
        color: #fff;
        cursor: pointer;
        position: relative;
    }
    .cart-num{
        position: absolute;
        top: -10px;
    }
    .anticon-shopping-cart{
        font-size: 20px;
    }
`